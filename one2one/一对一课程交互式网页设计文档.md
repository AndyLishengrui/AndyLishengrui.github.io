# 一对一门徒训练课程 - 交互式网页设计文档

## 📋 项目概述

### 项目背景
基于《一对一门徒训练》教材，为每个课程章节创建交互式网页，加入经文填空功能，帮助学员通过实践加深对圣经经文的记忆和理解。

### 设计目标
1. **互动性**：将静态文本转化为可交互的学习体验
2. **即时反馈**：提供答案检查和渐进提示功能
3. **多版本支持**：支持和合本、新译本等多个圣经版本
4. **进度追踪**：自动保存学习进度，显示完成统计
5. **响应式设计**：适配桌面和移动设备

---

## 🏗️ 系统架构

### 目录结构
```
one2one/
├── index.html                    # 课程首页/目录
├── courses/                      # 课程HTML文件
│   ├── one2one_C1_S1.html       # 第1课第1节
│   ├── one2one_C1_S2.html       # 第1课第2节
│   └── ...                       # 其他课程节
├── data/
│   └── answers/                  # 答案数据文件
│       ├── one2one_C1_S1.json   # 第1课第1节答案
│       └── ...                   # 其他答案数据
├── 一对一.txt                    # 原始课程文本
└── generate_all_pages.py         # 批量生成脚本
```

### 文件命名规范
- **HTML文件**：`one2one_C{课程号}_S{课内节号}.html`
  - 例：`one2one_C1_S1.html` = 第1课第1节
- **JSON文件**：`one2one_C{课程号}_S{课内节号}.json`
  - 例：`one2one_C1_S1.json` = 第1课第1节答案数据

---

## 📊 数据结构设计

### JSON答案数据格式

```json
{
  "course_num": 1,                    // 课程编号
  "course_title": "新起点 得救",       // 课程标题
  "section_num": 1,                    // 节号
  "section_title": "问题：罪使我们与神隔绝",  // 节标题
  "answers": {
    "q{题号}_{经文引用}": {
      "reference": "书卷名 章:节",    // 经文引用
      "text": "和合本经文内容",        // 默认答案（和合本）
      "version": "和合本",             // 默认版本
      "text_alt": "其他版本经文",      // 参考答案（可选）
      "version_alt": "新译本",         // 参考版本名称（可选）
      "has_data": true                 // 是否有答案数据
    }
  }
}
```

#### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `course_num` | Number | ✅ | 课程编号（1-7） |
| `course_title` | String | ✅ | 课程名称 |
| `section_num` | Number | ✅ | 章节编号 |
| `section_title` | String | ✅ | 章节标题 |
| `answers` | Object | ✅ | 答案集合 |
| `q{n}_{ref}` | Object | ✅ | 答案键名格式 |
| `reference` | String | ✅ | 经文引用格式："书卷 章:节" |
| `text` | String | ✅ | 和合本经文（默认答案） |
| `version` | String | ✅ | 版本标识（默认"和合本"） |
| `text_alt` | String | ❌ | 参考版本经文（如新译本） |
| `version_alt` | String | ❌ | 参考版本名称 |
| `has_data` | Boolean | ✅ | 标识是否有有效数据 |

#### 示例数据

```json
{
  "course_num": 1,
  "course_title": "新起点 得救",
  "section_num": 1,
  "section_title": "问题：罪使我们与神隔绝",
  "answers": {
    "q1_以赛亚书 59:1,2": {
      "reference": "以赛亚书 59:1,2",
      "text": "看哪，耶和华的膀臂并非缩短，不能拯救，耳朵并非发沉，不能听见，但你们的罪孽使你们与神隔绝；你们的罪恶使他掩面不听你们。",
      "version": "和合本",
      "version_alt": "新译本",
      "text_alt": "看哪！耶和华的手不是缩短了，以致不能拯救；他的耳朵不是不灵，不能听见；而是你们的罪孽使你们与你们的神隔绝；你们的罪恶使他掩面不顾你们，不听你们的祷告。",
      "has_data": true
    }
  }
}
```

---

## 🎨 页面设计规范

### 页面结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <!-- 元数据、标题、样式 -->
</head>
<body>
    <div class="container">
        <!-- 1. 页头区域 -->
        <header>
            <div class="breadcrumb">面包屑导航</div>
            <h1>课程标题</h1>
            <div class="section-title-box">节号和标题</div>
        </header>

        <!-- 2. 内容区域 -->
        <div class="content">
            <div class="question-block">
                <div class="question-header">问题文本</div>
                <div class="answers-area">
                    <div class="reference-with-blank">经文填空区</div>
                </div>
            </div>
        </div>

        <!-- 3. 操作栏 -->
        <div class="action-bar">
            <div class="score-display">成绩显示</div>
            <div>批改、提交、清空按钮</div>
        </div>

        <!-- 4. 导航区域 -->
        <div class="navigation">上一节/下一节按钮</div>
    </div>

    <!-- 5. 提示框和模态框 -->
    <div class="toast">消息提示</div>
    <div class="score-modal">评分报告</div>

    <!-- 6. JavaScript逻辑 -->
    <script>/* 交互逻辑 */</script>
</body>
</html>
```

### 核心组件设计

#### 1. 问题块 (question-block)
- **结构**：
  ```html
  <div class="question-block" data-question-id="1">
      <div class="question-header">
          <span class="question-number">1.</span>
          <span class="question-text">问题描述文本</span>
      </div>
      <div class="answers-area">
          <!-- 经文填空区域 -->
      </div>
  </div>
  ```
- **样式特点**：
  - 问题编号与文本行高一致（`line-height: 1.8`）
  - 字体大小：`1.1em`
  - 编号固定宽度：`min-width: 30px`

#### 2. 经文填空区 (reference-with-blank)
- **结构**：
  ```html
  <div class="reference-with-blank">
      <div class="reference-header">
          <span class="reference-text">以赛亚书 59:1,2</span>
          <div class="hint-buttons">
              <button class="btn-hint-partial">💡 提示</button>
              <button class="btn-hint-full">👁️ 答案</button>
          </div>
      </div>
      <textarea class="answer-input" 
                data-question="1" 
                data-reference="以赛亚书 59:1,2" 
                data-has-answer="true"
                data-hint-progress="0"
                placeholder="请填写经文内容（和合本）...">
      </textarea>
      <div class="answer-feedback"></div>
      <div class="standard-answer"></div>
  </div>
  ```
- **功能**：
  - 输入框自动保存
  - 渐进提示（每次显示10个字）
  - 显示多版本答案对照

#### 3. 答案反馈系统
- **三种状态**：
  ```css
  .answer-input.correct   /* 正确：绿色边框 */
  .answer-input.partial   /* 部分正确：橙色边框 */
  .answer-input.incorrect /* 错误：红色边框 */
  ```
- **判断标准**：
  - 相似度 ≥ 85%：✅ 正确
  - 相似度 ≥ 60%：⚠️ 部分正确
  - 相似度 < 60%：❌ 错误
  - 使用LCS（最长公共子序列）算法计算相似度

#### 4. 评分模态框 (score-modal)
- **显示内容**：
  - 总分（0-100分）
  - 评价消息（优秀/良好/及格/继续努力）
  - 详细统计：
    - 📊 总题数和答案框数
    - 📋 问题级别统计
    - 📝 答案框统计
    - 🎯 成绩计算说明
- **触发时机**：点击"📝 提交成绩"按钮

---

## 🎯 功能设计

### 1. 渐进提示功能

#### 功能描述
点击"💡 提示"按钮，每次显示答案的前N个字，帮助学员逐步回忆经文。

#### 实现逻辑
```javascript
function showPartialHint(questionId, ref) {
    const input = document.querySelector(`.answer-input[data-question="${questionId}"]`);
    const answerInfo = standardAnswers[`q${questionId}_${ref}`];
    const standardAnswer = answerInfo.text;
    
    let currentProgress = parseInt(input.dataset.hintProgress) || 0;
    currentProgress += 10;  // 每次显示10个字
    
    input.value = standardAnswer.substring(0, currentProgress);
    input.dataset.hintProgress = currentProgress;
    
    saveProgress();  // 自动保存
}
```

#### 用户体验
- 第1次点击：显示前10个字
- 第2次点击：显示前20个字
- ...
- 最后：显示完整答案并提示"已显示完整答案"

### 2. 多版本答案显示

#### 功能描述
点击"👁️ 答案"按钮，显示和合本和新译本（如有）的对照答案。

#### 显示格式
```html
<div class="standard-answer show">
    <div class="version-section">
        <div class="version-label">📖 和合本：</div>
        <div class="verse-text">看哪，耶和华的膀臂...</div>
    </div>
    <div class="version-section">
        <div class="version-label">📖 新译本：</div>
        <div class="verse-text">看哪！耶和华的手不是...</div>
    </div>
</div>
```

#### 实现逻辑
```javascript
function showFullHint(questionId, ref) {
    const answerInfo = standardAnswers[`q${questionId}_${ref}`];
    const answerDiv = document.querySelector(`.standard-answer[data-ref="${ref}"]`);
    
    let html = `<div class="version-section">
        <div class="version-label">📖 ${answerInfo.version}：</div>
        <div class="verse-text">${answerInfo.text}</div>
    </div>`;
    
    // 如果有其他版本
    if (answerInfo.version_alt && answerInfo.text_alt) {
        html += `<div class="version-section">
            <div class="version-label">📖 ${answerInfo.version_alt}：</div>
            <div class="verse-text">${answerInfo.text_alt}</div>
        </div>`;
    }
    
    answerDiv.innerHTML = html;
    answerDiv.classList.toggle('show');
}
```

### 3. 答案检查功能

#### 功能描述
点击"✓ 批改"按钮，检查所有填空的正确性并给出反馈。

#### 检查流程
1. 加载标准答案数据
2. 遍历所有输入框
3. 计算相似度（LCS算法）
4. 根据相似度标记状态
5. 定位到第一个错误位置
6. 显示统计信息

#### 相似度算法（LCS）
```javascript
function calculateSimilarity(text1, text2) {
    // 移除空格
    text1 = text1.replace(/\s+/g, '');
    text2 = text2.replace(/\s+/g, '');
    
    const len1 = text1.length;
    const len2 = text2.length;
    const dp = Array(len1 + 1).fill(0).map(() => Array(len2 + 1).fill(0));
    
    // 动态规划求最长公共子序列
    for (let i = 1; i <= len1; i++) {
        for (let j = 1; j <= len2; j++) {
            if (text1[i - 1] === text2[j - 1]) {
                dp[i][j] = dp[i - 1][j - 1] + 1;
            } else {
                dp[i][j] = Math.max(dp[i - 1][j], dp[i][j - 1]);
            }
        }
    }
    
    const lcs = dp[len1][len2];
    return lcs / Math.max(len1, len2);  // 返回0-1之间的相似度
}
```

### 4. 成绩提交与评分

#### 评分规则
```javascript
// 计算得分（0-100分）
const score = totalInputs > 0 
    ? Math.round(((correctInputs + partialInputs * 0.6) / totalInputs) * 100) 
    : 0;

// 评价标准
if (score >= 90) {
    message = '优秀！你掌握得非常好！';
} else if (score >= 75) {
    message = '良好！继续加油！';
} else if (score >= 60) {
    message = '及格！建议再复习一下';
} else {
    message = '继续努力！多读几遍经文吧';
}
```

#### 统计维度
1. **问题级统计**：
   - 每个问题的正确率
   - 准确率≥80%：✅ 正确
   - 准确率≥40%：⚠️ 部分正确
   - 准确率<40%：❌ 错误

2. **答案框统计**：
   - 总答案框数量
   - 完全正确数量
   - 部分正确数量
   - 错误数量

### 5. 自动保存功能

#### 保存时机
- 输入框内容变化时自动保存
- 使用localStorage存储
- 页面刷新后自动恢复

#### 实现代码
```javascript
// 保存进度
function saveProgress() {
    const progress = {};
    document.querySelectorAll('.answer-input').forEach(input => {
        const question = input.dataset.question;
        const ref = input.dataset.reference;
        const key = `${question}_${ref}`;
        progress[key] = input.value;
    });
    localStorage.setItem('one2one_C1_S1_progress', JSON.stringify(progress));
}

// 加载进度
function loadProgress() {
    const saved = localStorage.getItem('one2one_C1_S1_progress');
    if (saved) {
        const data = JSON.parse(saved);
        Object.keys(data).forEach(key => {
            const [question, ref] = key.split('_', 2);
            const input = document.querySelector(
                `.answer-input[data-question="${question}"][data-reference="${ref}"]`
            );
            if (input && data[key]) {
                input.value = data[key];
            }
        });
    }
}
```

---

## 🎨 样式设计规范

### 颜色系统

| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 主色调 | `#667eea` → `#764ba2` | 紫色渐变 |
| 成功色 | `#27ae60` | 绿色 |
| 警告色 | `#f39c12` | 橙色 |
| 错误色 | `#e74c3c` | 红色 |
| 信息色 | `#3498db` | 蓝色 |
| 文本色 | `#333` | 深灰 |
| 辅助文本 | `#666`, `#888` | 中灰、浅灰 |
| 背景色 | `#f8f9fa` | 浅灰背景 |

### 字体规范

| 元素 | 字体大小 | 行高 | 字重 |
|------|----------|------|------|
| 正文 | `16px` (基准) | `1.8` | `normal` |
| 问题文本 | `1.1em` (17.6px) | `1.8` | `normal` |
| 问题编号 | `1.1em` | `1.8` | `500` |
| 输入框 | `1.05em` | `1.8` | `normal` |
| 按钮 | `1em` | - | `500` |
| 大标题 | `2em` | `1.4` | `bold` |
| 节标题 | `1.3em` | `1.4` | `500` |

### 间距规范

| 类型 | 数值 | 说明 |
|------|------|------|
| 容器内边距 | `40px` | 桌面版 |
| 容器内边距 | `20px` | 移动版 |
| 问题间距 | `35px` | 问题之间 |
| 元素间距 | `15px` | 一般元素 |
| 小间距 | `8px` | 紧密元素 |
| 圆角 | `8px`, `15px` | 小圆角、大圆角 |

### 响应式断点

```css
@media (max-width: 768px) {
    /* 移动设备适配 */
    body { font-size: 15px; }
    .content { padding: 20px; }
    .answers-area { margin-left: 10px; }
    /* ... */
}
```

---

## 🔧 技术实现细节

### HTML数据属性

| 属性 | 用途 | 示例 |
|------|------|------|
| `data-question-id` | 问题块ID | `"1"` |
| `data-question` | 输入框对应问题号 | `"1"` |
| `data-reference` | 经文引用 | `"以赛亚书 59:1,2"` |
| `data-has-answer` | 是否有答案数据 | `"true"/"false"` |
| `data-hint-progress` | 提示进度 | `"0"`, `"10"`, `"20"` |
| `data-ref` | 反馈区域对应引用 | `"以赛亚书 59:1,2"` |

### JavaScript事件处理

```javascript
// 页面加载事件
window.addEventListener('load', () => {
    loadStandardAnswers();  // 加载答案数据
    loadProgress();         // 恢复进度
    updateProgress();       // 更新进度显示
});

// 输入框输入事件
document.querySelectorAll('.answer-input').forEach(input => {
    input.addEventListener('input', () => {
        saveProgress();     // 自动保存
        updateProgress();   // 更新进度
    });
});
```

### 异步数据加载

```javascript
async function loadStandardAnswers() {
    try {
        const response = await fetch('../data/answers/one2one_C1_S1.json');
        const data = await response.json();
        standardAnswers = data.answers || {};
        console.log('答案数据加载成功:', standardAnswers);
    } catch (error) {
        console.error('加载答案数据失败:', error);
    }
}
```

---

## 📱 用户体验设计

### 交互流程

```
用户访问页面
    ↓
加载答案数据 + 恢复进度
    ↓
阅读问题 → 填写答案 → [自动保存]
    ↓
[可选] 点击"💡 提示" → 渐进显示答案
    ↓
[可选] 点击"👁️ 答案" → 显示多版本对照
    ↓
点击"✓ 批改" → 即时反馈
    ↓
修改错误答案
    ↓
点击"📝 提交成绩" → 显示评分报告
    ↓
关闭报告 → [继续学习/下一节]
```

### 反馈机制

1. **即时反馈**：
   - Toast消息（3秒自动消失）
   - 输入框颜色变化
   - 文字提示信息

2. **视觉引导**：
   - 错误定位：自动滚动到第一个错误
   - 聚焦输入框
   - 按钮悬停效果

3. **鼓励机制**：
   - 完成度显示（X/Y）
   - 分数评价（优秀/良好/及格）
   - 详细统计数据

### 无障碍设计

- 语义化HTML标签
- 合适的对比度
- 键盘可访问
- 清晰的视觉层次

---

## 🔄 批量生成流程

### 生成脚本架构

```python
# 批量生成脚本结构
def main():
    # 1. 解析一对一.txt
    sections = parse_course_text('一对一.txt')
    
    # 2. 为每个章节：
    for section in sections:
        # 2.1 提取内容和经文
        content = extract_content(section)
        verses = extract_verses(section)
        
        # 2.2 获取和合本经文（API或备用）
        for verse in verses:
            cuv_text = get_bible_verse(verse, 'cuv')
            verse['text'] = cuv_text
            verse['text_alt'] = verse['original_text']
        
        # 2.3 生成JSON数据
        json_data = create_json_data(section, verses)
        save_json(json_data, section.filename)
        
        # 2.4 生成HTML页面
        html_content = create_html_page(section, verses)
        save_html(html_content, section.filename)
    
    print(f"✅ 完成！共生成 {len(sections)} 个页面")
```

### 关键函数

#### 1. 经文提取
```python
def extract_verses(section_text):
    """
    从章节文本中提取经文引用
    返回：[(引用, 原文, 版本), ...]
    """
    pattern = r'(\w+书?\s+\d+:\d+(?:[-,]\d+)?)\s*（(\w+)）'
    matches = re.findall(pattern, section_text)
    return matches
```

#### 2. 和合本获取
```python
def get_bible_verse(book, chapter, verse, version='cuv'):
    """
    从Bible API获取经文
    失败时返回None，使用备用经文
    """
    url = f"https://bible-api.com/{book}+{chapter}:{verse}?translation={version}"
    try:
        response = requests.get(url, timeout=10)
        if response.status_code == 200:
            return response.json().get('text', '').strip()
    except:
        return None
```

#### 3. HTML模板
```python
def create_html_page(section, verses):
    """
    使用模板生成HTML页面
    包含：样式、结构、JavaScript
    """
    template = load_template('page_template.html')
    return template.format(
        course_num=section.course_num,
        course_title=section.course_title,
        section_num=section.section_num,
        section_title=section.section_title,
        content=section.content,
        verses=format_verses(verses)
    )
```

---

## 📈 扩展性设计

### 支持的扩展功能

1. **更多圣经版本**：
   - 在JSON中添加更多`text_alt2`, `version_alt2`字段
   - 显示时循环遍历所有版本

2. **音频朗读**：
   - 添加音频文件链接
   - 播放按钮组件

3. **笔记功能**：
   - 为每个问题添加笔记区
   - 保存到localStorage或后端

4. **学习统计**：
   - 记录学习时长
   - 统计正确率趋势
   - 生成学习报告

5. **多用户支持**：
   - 后端API存储进度
   - 用户登录系统
   - 进度同步

---

## 🧪 测试清单

### 功能测试

- [ ] 页面正确加载
- [ ] 答案数据正确加载
- [ ] 输入框自动保存
- [ ] 渐进提示功能
- [ ] 多版本答案显示
- [ ] 答案批改准确性
- [ ] 成绩计算正确性
- [ ] 评分模态框显示
- [ ] 清空功能
- [ ] 导航链接有效

### 兼容性测试

- [ ] Chrome浏览器
- [ ] Firefox浏览器
- [ ] Safari浏览器
- [ ] Edge浏览器
- [ ] iOS Safari
- [ ] Android Chrome

### 响应式测试

- [ ] 桌面（1920x1080）
- [ ] 平板（768x1024）
- [ ] 手机（375x667）

### 性能测试

- [ ] 页面加载速度 < 2s
- [ ] JSON加载速度 < 500ms
- [ ] 输入响应流畅
- [ ] 相似度计算性能

---

## 🚀 部署指南

### 本地部署

```bash
# 1. 进入项目目录
cd /path/to/one2one

# 2. 启动HTTP服务器
python3 -m http.server 8765

# 3. 访问
open http://localhost:8765/courses/one2one_C1_S1.html
```

### 生产部署

1. **静态托管**（推荐）：
   - GitHub Pages
   - Netlify
   - Vercel

2. **Web服务器**：
   - Nginx配置
   - Apache配置

3. **CDN加速**：
   - 静态资源CDN
   - 图片优化

---

## 📝 维护指南

### 添加新章节

1. 在`一对一.txt`中添加内容
2. 运行生成脚本：
   ```bash
   python3 generate_new_section.py --course 1 --section 15
   ```
3. 手动验证生成的文件
4. 提交到Git

### 更新答案数据

1. 编辑对应的JSON文件
2. 刷新页面验证
3. 提交到Git

### 修改样式

1. 编辑HTML的`<style>`部分
2. 测试响应式效果
3. 如果是全局样式，提取到CSS文件

---

## 📚 参考资料

### 外部资源

- **Bible API**：https://bible-api.com/
- **LCS算法**：https://en.wikipedia.org/wiki/Longest_common_subsequence_problem
- **响应式设计**：https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Responsive_Design

### 项目文档

- `一对一.txt` - 原始课程文本
- `foundation_L1_S1.html` - 参考模板（根基课程）
- `foundation_L1_S1.json` - 参考数据格式

---

## 🎯 下一步计划

### 第一阶段：完成所有章节
- [ ] 生成第1课所有14节（已完成第1节）
- [ ] 生成第2-7课所有章节
- [ ] 统一测试所有页面

### 第二阶段：功能增强
- [ ] 添加课程首页导航
- [ ] 添加进度条显示
- [ ] 添加打印友好样式
- [ ] 优化移动端体验

### 第三阶段：用户体验
- [ ] 添加键盘快捷键
- [ ] 添加暗黑模式
- [ ] 添加字体大小调节
- [ ] 添加学习时间统计

---

## 📞 联系与反馈

如有问题或建议，请通过以下方式联系：
- GitHub Issues
- 项目维护者邮箱

---

**文档版本**：v1.0  
**最后更新**：2025年10月9日  
**作者**：Andy Lee  
**状态**：✅ 第一课第一节已完成并测试通过
